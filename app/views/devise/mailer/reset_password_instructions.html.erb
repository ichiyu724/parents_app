<p><%= @resource.email %>様</p>

<p>パスワードを再設定するためのリクエストを受けたためメールを送信しています。パスワードの再設定は以下のリンクから行うことができます。</p>

<p><%= link_to 'パスワード変更', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>パスワードの再設定をリクエストしていない場合は、このメールを無視してください。</p>
<p>上記のリンクにアクセスして新しいパスワードを設定するまでパスワードは変更されません。</p>

